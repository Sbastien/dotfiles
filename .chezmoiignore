# Patterns match TARGET paths, relative to $HOME — not source paths.
README.md
LICENSE
AGENTS.md
CLAUDE.md
mise.toml
committed.toml
# Gitignored, but chezmoi reads the source tree from disk and would deploy it.
docs
